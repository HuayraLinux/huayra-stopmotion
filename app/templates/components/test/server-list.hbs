<ul>
  {{#each servers as |server|}}
    <li>
      {{server.name}} ({{server.host}}:{{server.port}})
      {{test/remote-webcams server=server onSelect=(action (mut selectedWebcam))}}
    </li>
  {{/each}}
</ul>

<video src={{selectedWebcam}} autoplay/>
