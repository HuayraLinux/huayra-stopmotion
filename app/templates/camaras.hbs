<div class="contenedor">

  <div class="vbox header">

    <p>{{#link-to 'index' class="ui button"}}Regresar{{/link-to}}</p>

    <h1>Prueba de cámaras</h1>

    <p>Cantidad de cámaras disponibles: {{camaras.cantidadDeCamaras}}</p>

    {{#if hayCamaraSeleccionada}}
      <p>Hay una cámara seleccionada <button {{action "apagarCamara"}}>Apagar la camara seleccionada</button></p>
    {{else}}
      <button {{action "seleccionarCamara" 0}}>Inicializar camara 1</button>
    {{/if}}

    {{outlet}}

  </div>

  <div class="vbox vbox-camara">
    <div id='camara'>Contenendor para la cámara.</div>
  </div>

  <div class="vbox controles">
    <button class="ui button primary {{if hayCamaraSeleccionada '' 'disabled'}}" {{action "capturar"}}>Capturar</button>
    
    <button class="ui button red {{if haySeleccion '' 'disabled'}}" {{action "eliminarCuadrosSeleccionados"}}>Eliminar cuadros seleccionados</button>
  </div>

  <div class="vbox timeline">
    {{huayra-timeline capturas=capturas intervaloSeleccion=intervaloSeleccion}}
  </div>

</div>
